services:
  lumen:
    build: .
    container_name: lumen
    ports:
      - "${LUMEN_PORT:-7070}:70"
    environment:
      - PLEX_SERVER_URL=${PLEX_SERVER_URL:-http://192.168.1.50:32400}
    restart: unless-stopped
