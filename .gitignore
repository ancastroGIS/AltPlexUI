# Environment — contains API keys, never commit the filled-in version
.env

# WireGuard configs contain your private key — gitignored intentionally.
# See vpn/README.md for setup instructions.
vpn/*.conf

# Node / build artifacts
node_modules/
lumen/node_modules/
lumen/dist/

# OS
.DS_Store
Thumbs.db
